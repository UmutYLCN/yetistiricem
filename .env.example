# Copy to .env.local (git-ignored) and fill in. Read only by the server
# (Vite dev/preview middleware and `npm start`); never sent to the browser.
# Setup: README → "Oynatma listesi içe aktarma".
YOUTUBE_API_KEY=
